export interface CartItemOptions {
  showInput?: boolean;
  showActions?: boolean;
  showPrice?: boolean;
  showColor?: boolean;
  showSize?: boolean;
  showBorder?: boolean;
  shortDetail?: boolean;
  size?: string;
}
