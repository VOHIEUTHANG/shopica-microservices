import { IconDefinition } from '@ant-design/icons-angular';
import {
  UserOutline,
  LockOutline,
  ForwardFill,
  BackwardFill,
  CheckOutline,
  CaretUpOutline
} from '@ant-design/icons-angular/icons';

export const icons: IconDefinition[] = [ForwardFill, BackwardFill, UserOutline, CaretUpOutline, LockOutline, CheckOutline];
